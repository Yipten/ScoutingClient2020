<Page x:Class="ScoutingClient2020.Views.DataManagementView"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	  xmlns:vm="clr-namespace:ScoutingClient2020.ViewModels"
	  mc:Ignorable="d" 
	  d:DesignHeight="450" d:DesignWidth="800"
	  Title="DataManagementView">
	<Page.DataContext>
		<vm:DataManagementViewModel />
	</Page.DataContext>
	<Grid>
		<StackPanel Orientation="Vertical">
			<Label Content="Data Management" FontSize="24" />
			<Label Content="Bluetooth" FontSize="20" />
			<StackPanel Orientation="Horizontal">
				<Button Content="Receive File" Command="{Binding ReceiveFileCommand}" IsEnabled="{Binding !IsReceiving, Mode=OneWay}" />
				<Button Content="Cancel" Command="{Binding CancelCommand}" IsEnabled="{Binding IsReceiving, Mode=OneWay}" />
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<Label Content="Status: " />
				<!--TODO: bind to BTClient property-->
				<Label Content="" />
			</StackPanel>
			<Label Content="SQLite Databases" FontSize="20" />
			<!--TODO: bind command-->
			<Button Content="Merge" />
		</StackPanel>
	</Grid>
</Page>
